; Logical NOT demonstration for subleq VM (b = not a)

; ===========================
; code
; ===========================
subleq a b                  ; b = b - a
subleq one b -1             ;b = b - 1, halt program

; ===========================
; data
; ===========================
one: 1
Z:   0
a:   7
b:   0                       ; not 7 = -8
